import React from 'react';
import { Container, Typography } from '@mui/material';

const PricingPage = () => {
    return (
        <Container>
            <Typography variant="h2" gutterBottom>
                Árazás
            </Typography>
            <Typography variant="body1" gutterBottom>
                Itt található a pontosje árazása.
            </Typography>
        </Container>
    );
};

export default PricingPage;
